{
  "name": "clicky-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker proxy for Clicky — holds API keys, exposes /chat and /tts.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  }
}
